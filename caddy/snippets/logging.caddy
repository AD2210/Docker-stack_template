(common-log) {
    log {
        output file var/caddy/{args[0]}.access.log {
            roll_size 100MiB
            roll_keep 10
            roll_keep_for 720h
        }
        format json
    }
}

# Match the Server-setup shared snippet name for local validation.
(logging) {
    import common-log app
}
